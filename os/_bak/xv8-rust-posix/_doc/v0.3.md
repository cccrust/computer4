# v0.3 — 信號 (Signals)

> 核心信號框架，支援 SIGKILL/SIGTERM/SIGINT 等。

## 信號常數

```rust
pub const NSIG: usize = 32;

pub enum Signal {
    SIGHUP  = 1,   // Hangup
    SIGINT  = 2,   // Interrupt (Ctrl-C)
    SIGQUIT = 3,   // Quit
    SIGILL  = 4,   // Illegal instruction
    SIGTRAP = 5,   // Trace/breakpoint
    SIGABRT = 6,   // Abort
    SIGBUS  = 7,   // Bus error
    SIGFPE  = 8,   // Floating point exception
    SIGKILL = 9,   // Kill (cannot be caught/ignored)
    SIGUSR1 = 10,  // User-defined 1
    SIGSEGV = 11,  // Segmentation fault
    SIGUSR2 = 12,  // User-defined 2
    SIGPIPE = 13,  // Broken pipe
    SIGALRM = 14,  // Alarm clock
    SIGTERM = 15,  // Termination
    SIGCHLD = 17,  // Child status changed
    SIGCONT = 18,  // Continue
    SIGSTOP = 19,  // Stop (cannot be caught/ignored)
    SIGTSTP = 20,  // Keyboard stop
}
```

## 核心結構變更

### ProcInner 新增欄位

```rust
pub struct ProcInner {
    // ...現有欄位...
    pub sigactions: [SigAction; NSIG],  // 信號處理器表
    pub pending: u32,                   // 擱置信號 bitmask
    pub blocked: u32,                   // 遮罩信號 bitmask
}

pub struct SigAction {
    pub handler: usize,  // SIG_DFL=0, SIG_IGN=1, or function pointer
    pub flags: u32,
    pub mask: u32,
}
```

### 預設動作表

| 信號 | 預設動作 |
|------|----------|
| SIGKILL (9) | Terminate |
| SIGSTOP (19) | Stop |
| SIGCHLD (17), SIGCONT (18) | Ignore |
| 其餘 | Terminate |

## 新增系統呼叫

```rust
Syscall::Sigaction  = 37,  // int sigaction(int sig, SigAction *act, SigAction *oldact)
Syscall::Sigprocmask = 38, // int sigprocmask(int how, u32 *set, u32 *oldset)
Syscall::Sigpending  = 39, // int sigpending(u32 *set)
Syscall::Sigsuspend  = 40, // int sigsuspend(u32 mask)
```

## 信號遞送機制

在 `usertrap()` 返回使用者空間前：

1. 檢查 `pending & ~blocked`
2. 若無擱置信號 → 正常返回使用者空間
3. 若有信號：
   - **SIGKILL / 預設動作 = terminate** → `proc::exit(-1)`
   - **SIGSTOP / 預設動作 = stop** → 行程進入 `Sleeping` 直到收到 SIGCONT
   - **SIG_IGN** → 清除 pending bit，繼續
   - **有自訂 handler** → 在使用者堆疊上建立 sigframe，設定 sepc 為 handler 位址
   - 清除 pending bit

### sigframe 結構

```rust
struct SigFrame {
    return_addr: usize,  // sa_sigaction 返回位址（sigreturn trampoline）
    epc: usize,          // 原始 sepc
    ra: usize,
    a0: usize,           // sigaction 的 arg
    // ... 其他需要保存的暫存器
}
```

## 任務

- [x] **`kernel/src/signal.rs`** — `Signal` enum、`SigAction` struct、`NSIG`、預設動作表
- [x] **`ProcInner` 擴充** — 新增 `sigactions`、`pending`、`blocked` 欄位
- [x] **`sys_sigaction`** — 查/設信號處理器（SIGKILL/SIGSTOP 回 EINVAL）
- [x] **`sys_sigprocmask`** — SIG_BLOCK/SIG_UNBLOCK/SIG_SETMASK
- [x] **`sys_sigpending`** — 回傳 pending 信號遮罩
- [x] **`sys_sigsuspend`** — 以新遮罩 atomically 等待信號
- [x] **信號遞送** — `usertrap()` 返回前檢查 `pending & ~blocked`
- [x] **`sys_kill 改寫`** — 接受 `(pid, sig)` 引數，設 `pending` bit
- [x] **預設動作** — terminate / ignore / stop
- [x] **Ctrl-C → SIGINT** — UART 中斷處理發送 SIGINT 給前景行程
- [x] **`_signal` 測試** — 含 SIGALRM 自我信號、SIG_IGN、自訂 handler
- [x] **信號編號註冊** — `Syscall` enum 新增 4 個信號 syscall
- [x] **使用者包裝層** — `user/src/syscall.rs` 新增信號 wrapper

## 交付標準

- [x] `./test.sh` — 原有 8 項 + `_signal` 全部通過
- [x] `kill(pid, SIGKILL)` 可終止行程
- [x] 自訂 `SIGALRM` handler 可正確執行
- [x] Ctrl-C 發送 SIGINT 給前景行程
- [x] `sigprocmask(SIG_BLOCK, ..)` 可延遲信號遞送
